name: CI/CD pipeline

on:
  push:

jobs:
  tests:
    name: "Tests"
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
  
    - uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '9.x'

    - uses: actions/cache@v4
      with:
        path: |
          ~/.nuget/packages
          ~/.dotnet/packs
          ~/.dotnet/workloadmanifests
          ~/.dotnet/sdk-manifests
        key: ${{ runner.os }}-dotnet-${{ hashFiles('**/*.csproj') }}
        restore-keys: |
          ${{ runner.os }}-dotnet-

    - name: Restore solution
      run: |
          dotnet workload restore
          dotnet restore

    - name: Run tests
      run: dotnet test --no-restore --verbosity normal --logger "trx;LogFileName=tests-results.trx"

    - name: Publish test reports
      uses: dorny/test-reporter@v2
      if: ${{ !cancelled() }}
      with:
         name: Tests
         path: '**/tests-results.trx'
         reporter: dotnet-trx

    # - name: Build signed AAB
    #   run: |
    #     dotnet publish ScheduleMADI/ScheduleMADI.csproj \
    #       -c Release -f net9.0-android35.0 \
    #       -p:AndroidPackageFormat=aab \
    #       -p:AndroidSigningKeyStore=keystore.jks \
    #       -p:AndroidSigningStorePass=${{ secrets.AAB_KEYSTORE_PASSWORD }} \
    #       -p:AndroidSigningKeyAlias=${{ secrets.AAB_KEY_ALIAS }} \
    #       -p:AndroidSigningKeyPass=${{ secrets.AAB_KEY_PASSWORD }} 

    # - name: Upload APK artifact
    #   uses: actions/upload-artifact@v4
    #   with:
    #     name: apk
    #     path: ScheduleMADI/bin/Release/net9.0-android35.0/publish/com.wildchair.schedulemadi-Signed.apk